fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
requests>=2.32.0
python-dotenv>=1.0.0
reportlab>=4.2.2
slowapi>=0.1.9
Pillow>=10.3.0
opencv-python>=4.9.0
numpy>=1.26,<3
scipy>=1.11

# Module 1 & 2 — CNN classifiers, inference (needed at runtime)
torch>=2.2.0
torchvision>=0.17.0

# Module 3 — gemstone detector, inference (needed at runtime)
ultralytics>=8.2.0

# Module 4 — CLIP similarity search, inference (needed at runtime)
transformers>=4.40.0
